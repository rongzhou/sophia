{
  "type": "object",
  "additionalProperties": false,
  "required": ["purpose", "pseudocode"],
  "properties": {
    "purpose": { "type": "string", "minLength": 1 },
    "pseudocode": {
      "type": "string",
      "minLength": 1,
      "description": "完整 .pseudo 正文。必须包含独立行：<!-- sophia-pseudo: v1 -->、# Purpose、# Inputs、# Outputs、# Algorithm、# Constraints、# Forbidden。"
    },
    "libraries": {
      "type": "array",
      "items": { "type": "string" },
      "description": "本方案要用到的标准库名（从库目录中选；不用库则空数组）。"
    }
  }
}
