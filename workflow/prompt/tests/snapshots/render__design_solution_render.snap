---
source: workflow/prompt/tests/render.rs
expression: out
---
# 设计方案：编写结构化 .pseudo

为目标域编写 paper 风格的结构化伪代码（`.pseudo`），稳定算法意图。
**不要**直接写 Sophia-Core 源码、完整类型签名或 formal effect 名。

## 目标
实现 CompleteTodo

## 约束
- 不得访问 Users 表


## 验收条件
- 完成后 status == Done


## Action-rooted 语义上下文
- domains/TodoDomain/entities/Todo.sophia


## 可用标准库（如本方案需要某库功能，在 libraries 中列出其名）
- `example_lib` — 中立示例库

## 输出要求
输出包含六个固定 heading 的 Markdown：Purpose / Inputs / Outputs / Algorithm /
Constraints / Forbidden。首行写 `<!-- sophia-pseudo: v1 -->`。
若本方案要用到上面列出的标准库，在结构化输出的 `libraries` 字段列出所选库名（不用则空数组）；
伪代码正文用业务语言描述意图即可，**不必**写出库的具体操作签名（那是实现阶段的事）。
