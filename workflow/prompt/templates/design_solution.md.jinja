# 设计方案：编写语义伪代码

为目标域编写语义层伪代码，稳定业务意图与可观察行为。
**不要**直接写 Sophia-Core 源码、语言级声明、库操作签名、文件路径或文件布局。

## 目标
{{ objective }}

## 约束
{% for c in constraints %}- {{ c }}
{% endfor %}

## 验收条件
{% for a in acceptance_criteria %}- {{ a }}
{% endfor %}

## Action-rooted 语义上下文
{% for f in context_files %}- {{ f }}
{% endfor %}

## 可用标准库（如本方案需要某库功能，在 libraries 中列出其名）
{{ stdlib_catalog }}

## 输出要求
若本方案要用到上面列出的标准库，在结构化输出的 `libraries` 字段列出所选库名（不用则空数组）；
伪代码正文用业务语言描述意图、输入含义、输出含义、关键业务规则和禁止事项即可。
**不必也不应该**写出 Sophia 语法、库的具体操作签名、语言级声明或文件组织；
这些都是实现阶段的事。
