Your previous response could not be parsed as the required JSON object.

Return the same answer again as valid JSON only. Do not add markdown fences, prose, or commentary. Do not change the semantic content except to fix JSON formatting.

Parse error:
{{parse_error}}

Original instruction:
{{original_prompt}}

Invalid response:
{{invalid_response}}
